[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers --tb=short -m "not slow"
markers =
    slow: long-running performance, memory dump, or stress validation tests
filterwarnings =
    ignore:datetime.datetime.utcnow\(\) is deprecated:DeprecationWarning
